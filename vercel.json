{
  "version": 2,
  "builds": [
    { "src": "server/server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "server/server.js" },
    { "src": "/(.*)", "dest": "public/$1", "check": true },
    { "src": "/(.*)", "status": 200, "dest": "public/index.html" }
  ]
}